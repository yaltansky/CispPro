USE CISP_SHARED
GO
IF NOT EXISTS (SELECT * FROM sys.objects WHERE object_id = OBJECT_ID(N'[EVENTS_PRIORITIES]') AND type in (N'U'))
BEGIN
CREATE TABLE [EVENTS_PRIORITIES](
	[PRIORITY_ID] [int] NOT NULL,
	[NAME] [nvarchar](30) NULL,
	[CSS_CLASS] [varchar](50) NULL,
 CONSTRAINT [PK_EVENTS_PRIORITIES] PRIMARY KEY CLUSTERED 
(
	[PRIORITY_ID] ASC
)
)
END
GO
